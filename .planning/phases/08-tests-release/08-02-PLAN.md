---
phase: 08-tests-release
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - CHANGELOG.md
  - package.json
autonomous: true
requirements: [REL-01]

must_haves:
  truths:
    - "CHANGELOG.md exists with v0.2.0 entry covering all audit fixes"
    - "package.json version is 0.2.0"
    - "Git tag v0.2.0 exists on HEAD"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "Release changelog with v0.2.0 section"
      contains: "## v0.2.0"
    - path: "package.json"
      provides: "Version bump to 0.2.0"
      contains: "\"version\": \"0.2.0\""
  key_links:
    - from: "CHANGELOG.md"
      to: "git tag v0.2.0"
      via: "release commit tagged"
      pattern: "v0.2.0"
---

<objective>
Create CHANGELOG.md, bump version to 0.2.0, and create git tag v0.2.0.

Purpose: REL-01 requires a proper tagged release. CHANGELOG documents all v0.2.0 audit fixes (Phases 5-8) for users and future maintainers.

Output: CHANGELOG.md, updated package.json, git tag v0.2.0.
</objective>

<execution_context>
@/home/ai/.claude/get-shit-done/workflows/execute-plan.md
@/home/ai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-tests-release/08-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CHANGELOG.md and bump version</name>
  <files>CHANGELOG.md, package.json</files>
  <action>
1. Create CHANGELOG.md at project root with this structure:

```
# Changelog

## v0.2.0 — Hardening & Honesty (YYYY-MM-DD)

Addresses all findings from external Codex/GPT audit.

### Fixed
- **Sync correctness**: embedding sync no longer silently loses work — failed embeds are tracked in `sync_failures` table with retry (up to 5 attempts) and park logic
- **Backfill correctness**: `embeddingBackfill` now checks `response.ok` — non-200 responses no longer count as success
- **README honesty**: removed misleading "zero infrastructure" claims; engram-vec and Ollama dependencies now honestly documented
- **License alignment**: README, package.json, and LICENSE all consistently say MIT

### Added
- `sync_failures` table: tracks failed embeds with attempt count, error, timestamps, pending/parked status
- Sync health reporting in `cortex_status`: pending count, parked count, oldest failure, total retries
- Debug/explain scoring mode: opt-in `explain` parameter on `cortex_search` shows per-result FTS, vector, graph, lifecycle, and recency score breakdown
- Runtime mode control via `CORTEX_MODE` env var: `readonly`, `default`, `backfill-once`, `debug`
- Test coverage for ranking order, confidence decay, cache invalidation, sync failure/retry
- CHANGELOG.md

### Changed
- `filterLifecycle` returns `{kept, excluded}` object (backward-compatible)
- Version bump: 0.1.0 -> 0.2.0

## v0.1.0 — MVP (2026-03-25)

Initial release with MCP server, hybrid FTS5+vector search, preflight briefs, knowledge graph, and embedding sync.
```

Use actual date for v0.2.0.

2. Bump version in package.json from "0.1.0" to "0.2.0".
  </action>
  <verify>
    <automated>cd /home/ai/cortex && cat CHANGELOG.md | head -5 && node -e "console.log(require('./package.json').version)"</automated>
  </verify>
  <done>CHANGELOG.md exists with v0.2.0 entry. package.json version is 0.2.0.</done>
</task>

<task type="auto">
  <name>Task 2: Commit and tag v0.2.0</name>
  <files></files>
  <action>
1. Stage CHANGELOG.md and package.json.
2. Commit with message: `chore: release v0.2.0 — hardening & honesty`
3. Create annotated git tag: `git tag -a v0.2.0 -m "v0.2.0: Hardening & Honesty — sync correctness, README honesty, debug mode, runtime modes, full test coverage"`
4. Verify: `git tag -l v0.2.0` returns the tag, `git log --oneline -1` shows the release commit.
  </action>
  <verify>
    <automated>cd /home/ai/cortex && git tag -l v0.2.0 && git log --oneline -1</automated>
  </verify>
  <done>Git tag v0.2.0 exists on the release commit. `git tag -l v0.2.0` outputs "v0.2.0".</done>
</task>

</tasks>

<verification>
- `cat CHANGELOG.md` shows v0.2.0 section with all audit fixes documented
- `node -e "console.log(require('./package.json').version)"` outputs "0.2.0"
- `git tag -l v0.2.0` outputs "v0.2.0"
- `git log --oneline -1` shows release commit
</verification>

<success_criteria>
- CHANGELOG.md covers: sync correctness fix, backfill fix, README honesty, license alignment, sync_failures table, sync health reporting, debug/explain mode, runtime modes, test coverage additions
- package.json version is 0.2.0
- Annotated git tag v0.2.0 exists
</success_criteria>

<output>
After completion, create `.planning/phases/08-tests-release/08-02-SUMMARY.md`
</output>
