---
phase: 08-tests-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tests/search.test.ts
  - src/tests/preflight.test.ts
  - src/tests/config.test.ts
  - src/tests/status.test.ts
  - src/tests/logger.test.ts
autonomous: true
requirements: [TEST-01]

must_haves:
  truths:
    - "All 149+ existing tests pass (zero regressions)"
    - "Ranking order test proves composite score sorts correctly: higher relevance + recency beats lower"
    - "Cache invalidation test proves preflight returns changed=true after new observation inserted"
  artifacts:
    - path: "src/tests/search.test.ts"
      provides: "Ranking order tests for composite scoring"
      contains: "composite.*score.*order"
    - path: "src/tests/preflight.test.ts"
      provides: "Cache invalidation test on new observation"
      contains: "cache.*invalidat"
  key_links:
    - from: "src/tests/search.test.ts"
      to: "src/core/search.ts"
      via: "enrichWithLifecycle composite scoring"
      pattern: "compositeScore.*lcConfidence.*recencyBoost"
    - from: "src/tests/preflight.test.ts"
      to: "src/core/preflight.ts"
      via: "hash-based cache dedup"
      pattern: "brief_hash"
---

<objective>
Fix 4 pre-existing test failures and add missing test coverage for ranking order and cache invalidation.

Purpose: TEST-01 requires tests for ranking order, confidence decay, cache invalidation, and sync failure/retry. Confidence decay (lifecycle.test.ts: 7 tests) and sync failure/retry (sync.test.ts: 18 tests) are already well-covered from Phase 5. This plan fills the two gaps: ranking order and cache invalidation. It also fixes 4 pre-existing test failures so all tests pass green.

Output: All tests passing, new tests for ranking order and cache invalidation.
</objective>

<execution_context>
@/home/ai/.claude/get-shit-done/workflows/execute-plan.md
@/home/ai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/core/search.ts
@src/core/preflight.ts
@src/core/lifecycle.ts

<interfaces>
<!-- Composite scoring formula from src/core/search.ts enrichWithLifecycle(): -->
```typescript
// Recency boost: 1 + 0.5 * exp(-ageDays/30)
const recencyBoost = 1 + 0.5 * Math.exp(-ageDays / 30);
// Composite score = base_score * lifecycle_confidence * recency_boost
const compositeScore = r.score * lcConfidence * recencyBoost;
```

<!-- Cache dedup from src/core/preflight.ts: -->
```typescript
// Hash-based: briefHash = sha256(brief).slice(0,16)
// Cache hit when: cached.brief_hash === briefHash AND expires_at > now
// New observation changes brief content -> different hash -> cache miss -> changed=true
```

<!-- computeConfidence from src/core/lifecycle.ts: -->
```typescript
export function computeConfidence(params: {
  status: string;
  confidence: number;
  created_at: string;
  last_verified_at: string | null;
}): number;
// Returns 0 for deprecated/superseded
// Decays with 180-day half-life for active
// Boosts for recent verification
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix 4 pre-existing test failures</name>
  <files>src/tests/config.test.ts, src/tests/status.test.ts, src/tests/logger.test.ts</files>
  <action>
Fix 4 failing tests. Read each file, diagnose the failure, and fix:

1. **config.test.ts** -- "config reads from environment variables when set" fails with "Unexpected end of JSON input". The test spawns a subprocess to read config with env vars set. The subprocess likely outputs non-JSON or the import path changed. Read the test, read src/config.ts, and fix the subprocess invocation or assertion.

2. **status.test.ts** -- "reports engram-vec as down when unavailable" expects 'down' but gets 'ok'. The test likely needs the engram-vec URL pointed to an unreachable endpoint (similar to search tests). Read the test, check if it properly forces the URL to be unreachable before checking status.

3. **logger.test.ts** -- Two tests fail: "log() stderr output includes timestamp and level prefix" and "log() with info level shows INFO prefix". The logger output format likely changed (Phase 7 may have modified it). Read src/lib/logger.ts and the test, update assertions to match current output format.

For each fix: make the minimal change needed. Do NOT change the behavior being tested, only align the test with the actual implementation.

Run `npx tsx --test src/tests/config.test.ts src/tests/status.test.ts src/tests/logger.test.ts` after each fix to verify.
  </action>
  <verify>
    <automated>cd /home/ai/cortex && npx tsx --test src/tests/config.test.ts src/tests/status.test.ts src/tests/logger.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>All 4 previously-failing tests now pass. Zero new failures introduced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add ranking order and cache invalidation tests</name>
  <files>src/tests/search.test.ts, src/tests/preflight.test.ts</files>
  <behavior>
    - Test: "composite score orders results by relevance * confidence * recency" -- Create 3 observations with controlled created_at dates and lifecycle states. A recent active observation should rank above an older active observation with same FTS relevance. Verify the returned text shows the recent one first.
    - Test: "older observation with higher FTS rank still wins when recency difference is small" -- Two observations created within same day, one with better FTS match. Verify FTS relevance dominates when recency is similar.
    - Test: "composite ranking degrades stale observations below fresh ones" -- One observation from 360+ days ago (very decayed confidence) vs one from today. Today's should rank first even if FTS rank is slightly worse.
    - Test: "preflight cache invalidates when new observation is added" -- Call generatePreflight (gets changed=true, caches). Insert a NEW observation for same project. Call generatePreflight again. Should return changed=true (not the cached "No changes" response) because the new observation changes the brief hash.
    - Test: "preflight cache remains valid when no new observations added" -- Call generatePreflight twice without DB changes. Second call returns changed=false (existing test confirms this).
  </behavior>
  <action>
Add to search.test.ts under the existing "lifecycle-aware search" describe block:

1. "composite score orders results: recent active > old active at same FTS relevance" -- Insert two observations with identical FTS-matching content but different created_at (one today, one 200 days ago). Both active. Search, verify today's observation appears first in output.

2. "composite ranking: very stale observation ranks below fresh one" -- Insert obs from 400 days ago and obs from today. Search for a term both match. Verify fresh ranks above stale.

Add to preflight.test.ts:

3. "cache invalidates when new observation is inserted" -- Create test DB, call generatePreflight (first call: changed=true), then INSERT a new observation for the same project, call generatePreflight again, assert changed=true (cache invalidated because brief content changed).

Use the existing createTestDb/createPreflightTestDb helpers. Follow the existing test patterns exactly (node:test, assert/strict, in-memory DB, cleanup).
  </action>
  <verify>
    <automated>cd /home/ai/cortex && npx tsx --test src/tests/search.test.ts src/tests/preflight.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>New ranking order tests prove composite score produces correct sort. Cache invalidation test proves preflight detects new observations. All search and preflight tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Full test suite green confirmation</name>
  <files></files>
  <action>
Run the complete test suite: `npx tsx --test src/tests/*.test.ts`

Verify:
- Total tests >= 153 (149 existing + 4 new minimum)
- 0 failures
- 0 cancelled

If any failures remain, diagnose and fix them. The goal is 100% pass rate.

Output the final test count for the SUMMARY.
  </action>
  <verify>
    <automated>cd /home/ai/cortex && npx tsx --test src/tests/*.test.ts 2>&1 | grep -E "^# (tests|pass|fail)"</automated>
  </verify>
  <done>Full test suite passes with 0 failures. Test count >= 153.</done>
</task>

</tasks>

<verification>
- `npx tsx --test src/tests/*.test.ts` exits with 0 failures
- New ranking order tests exist in search.test.ts
- New cache invalidation test exists in preflight.test.ts
- Test count >= 153 (149 existing + new tests)
</verification>

<success_criteria>
- All pre-existing test failures are fixed
- Ranking order is tested with composite score verification
- Cache invalidation is tested with new observation insertion
- Full suite: 0 failures, 0 cancelled
</success_criteria>

<output>
After completion, create `.planning/phases/08-tests-release/08-01-SUMMARY.md`
</output>
